package info.phosco.forms.translate.element.recordgroup.value;

public enum Datatype {

	// TODO: if we can reuse the class for the Trigger codes, enhance it. If
	// not, we can use ColumnDatatype instead.
	DATE,
	CHARACTER,
	NUMBER;

}
